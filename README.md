# TaskFriend
This project is a desktop productivity app for task management built with Python, Tkinter, Matplotlib, and SQLite.

Install required packages with the following command:  
`pip install -r requirements.txt`

Inside the project directory, run:  
`python main.py`

You will be taken to the login window of the app.
